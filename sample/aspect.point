import "github.com/sirupsen/logrus"
import "github.com/Justice-love/go-aspect/test"
import "context"


point after(main.Do(c Context)) {
	test.AfterPrint({{c}})
}

point before(main.Do(c Context)) {
	{{c}} = test.BeforePrint({{c}})
}
